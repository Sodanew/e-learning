import 'package:flutter/widgets.dart';

class CourseReviewsTabWidget extends StatelessWidget {
  const CourseReviewsTabWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
